On a new database, you must import, in this order:
schema.sql (tables structure)
session.sql (table session, needed by CodeIgniter)
config.sql (will insert config values)

If you want some mock data, import data.sql
Then you can log using admin/admin

schema.xml is the schema for the visual designer tool.